params.outdir = 's3://altmx-artemis/data/pubmed/'
params.env = 'prod'

process {
    container = 'public.ecr.aws/h8y0t9h1/pubmed:latest'
}

profiles {
    local {
        includeConfig 'conf/base.config'
        includeConfig 'conf/local.config'
    }
    seqera {
        includeConfig 'conf/base.config'
    }
}
